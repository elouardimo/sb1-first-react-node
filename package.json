{
  "name": "react-node-starter",
  "version": "1.0.0",
  "description": "Application de démarrage avec React et Node.js",
  "main": "index.js",
  "scripts": {
    "start": "npm run build-client && npm run server",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "build-client": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install"
  },
  "keywords": ["react", "node", "starter"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}